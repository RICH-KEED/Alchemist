# Performance Profiling Report

> Generated by Performance Profiler (#39) on `<date>` from `<trace-source>`.
> App: `<app-name>` · Flutter `<version>` · Device: `<device-model>` / `<OS-version>`

---

## 1. Summary

| Metric | Value | Budget | Status |
|---|---|---|---|
| Frames profiled | `<count>` | — | — |
| Janky frames | `<count>` (`<percent>%`) | < 5% | `<status>` |
| Avg frame time | `<ms>` | 16.67 ms | `<status>` |
| Max frame time | `<ms>` | 16.67 ms | `<status>` |
| Avg rebuilds / frame | `<count>` | < 50 | `<status>` |
| Avg raster time | `<ms>` | < 8 ms | `<status>` |
| Shader compilations detected | `<count>` | 0 | `<status>` |
| GC pauses | `<count>` | < 1 / 100 frames | `<status>` |

---

## 2. Frame budget violations

Each section below captures one violation. Order by impact: estimated ms saved x frames affected.

### Violation #1: `<headline>`

- **Frame range:** frames `<start>`–`<end>` (timestamps `<t1>`–`<t2>`)
- **Jank category:** Dart VM / Layout / Rasterization / Shader compilation
- **Thread:** UI thread / Raster thread / Both
- **Root cause:** `<which signal from SKILL.md §3>`
- **Affected widget / file:** `<ClassName>` in `<path/to/file.dart>`
- **Fix recommendation:** `<specific code change — add const, add select, RepaintBoundary, Isolate, etc.>`
- **Estimated improvement:** `<range> ms per frame`
- **Confidence:** HIGH / MEDIUM / LOW
- **Before screenshot:** <!-- placeholder: attach DevTools timeline highlighting the violation -->
- **After screenshot:** <!-- placeholder: attach timeline after fix showing the frame within budget -->

### Violation #2: `<headline>`

- **Frame range:** frames `<start>`–`<end>`
- **Jank category:** `<category>`
- **Thread:** `<thread>`
- **Root cause:** `<signal>`
- **Affected widget / file:** `<ClassName>` in `<path>`
- **Fix recommendation:** `<change>`
- **Estimated improvement:** `<range> ms per frame`
- **Confidence:** `<confidence>`
- **Before screenshot:** <!-- placeholder -->
- **After screenshot:** <!-- placeholder -->

<!-- Repeat for each violation. Delete this comment. -->

---

## 3. Rebuild audit

List every widget that rebuilds > 10 times per janky frame, with the trigger.

| Widget | File | Rebuilds / frame | Trigger (provider / setState / parent) | Fix |
|---|---|---|---|---|
| `<ClassName>` | `<file.dart>` | `<count>` | `<trigger>` | `<select / const / extract / RepaintBoundary>` |

---

## 4. Action plan (prioritized)

| # | Fix | Files changed | Est. ms saved | Effort | Risk | Rollback |
|---|---|---|---|---|---|---|
| 1 | `<fix description>` | `<file.dart>` | `<ms>` | LOW / MED / HIGH | LOW / MED / HIGH | `<revert steps>` |
| 2 | `<fix>` | `<files>` | `<ms>` | `<effort>` | `<risk>` | `<rollback>` |

---

## 5. Shader warm-up status

- SkSL captured: YES / NO
- SkSL bundled in release: YES / NO
- Shader compilation events in trace: `<count>`
- Action: `<bundle and add to CI / already bundled / not applicable>`

---

## 6. Isolate offload recommendations

| Compute function | UI thread time | File | Offload to | Est. saving |
|---|---|---|---|---|
| `<function name>` | `<ms>` | `<file.dart>` | `Isolate.run` / `compute()` | `<ms>` |

---

## 7. Follow-up

- [ ] Apply fix #1 and re-profile
- [ ] Apply fix #2 and re-profile
- [ ] Run pre-ship checklist from `perf_checklist.md`
- [ ] Re-run the scenario that triggered the trace; confirm all frames within 16ms budget
- [ ] File regression test (e.g., `flutter test --profile` assertion on frame budget)
