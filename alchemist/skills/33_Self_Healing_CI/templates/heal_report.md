# Self-Heal Report — <run id / branch>

Output template for Self-Healing CI (#33). Emit this whether the loop ends green or gives up — it is
the resume point and the human escalation summary. Post it as the PR/issue comment (the workflow in
`self_heal.yaml` can do this once you opt in).

---

**Run:** #<run-id> · **Branch:** `<branch>` · **CI workflow:** <name> · **Date:** <YYYY-MM-DD>
**Verdict:** `HEALED ✅` | `ESCALATED ⛔`
**Attempts:** `<n> / <N=3>`

---

## 1. Failure class

The family (or families, in peel order) from [`failure_taxonomy.md`](failure_taxonomy.md):

| # | Attempt | Family | Telltale log line | Fixer / action routed to |
|---|---|---|---|---|
| 1 | first  | <build/lint/format/test-flake/version-solve/signing/timeout/network> | `<the matched line>` | <#37 / #41 / re-run / pin / config> |
| 2 | re-run | <family that surfaced after the first fix> | `<matched line>` | <fixer> |
| 3 | …      | … | … | … |

> Note any **peel**: e.g. "fixed the namespace (build), which revealed a version-solve error
> underneath." Re-classification each pass is expected.

## 2. Action taken

For each attempt, what ran and what it changed:

- **Attempt 1 — <family>:** Routed to <fixer>. Diagnosis: `<diagnose.py top cause / #41 verdict>`.
  Change: `<file:line edit / commit sha / "re-run only, no edit">`. Commit: `<sha>` on `<branch>`
  (never main; never merged).
- **Attempt 2 — <family>:** …
- **Attempt 3 — <family>:** …

## 3. Attempts & re-run outcomes

| Attempt | Action | Re-run result |
|---|---|---|
| 1 | <fix/re-run> | <green / red-same / red-new-family> |
| 2 | … | … |
| 3 | … | … |

Cap: stopped at `<n>/<N>`. <If N reached: "attempt cap hit — escalated rather than looping.">

## 4. Result

### If HEALED ✅
- The re-run passed (`gh run watch --exit-status` → 0). It was **<family>**, fixed by **<action>**.
- Commit(s) awaiting **human merge** — the agent did NOT auto-merge.
- Follow-ups: <e.g. "quarantined flaky test `foo_test.dart`; tracking issue #123">.

### If ESCALATED ⛔
- Could not safely auto-heal. Reason: <one of>
  - Build Doctor (#37) classified it as **plugin/upstream** or **environment** — not a repo edit.
  - Analyzer Auto-Fix (#41) returned a **JUDGMENT** lint: `severity • rule • file:line`.
  - **Missing secret** — add `<SECRET_NAME>` (agent cannot create secrets).
  - **Deterministic test failure** — real regression in `<test>`, route to <owner>.
  - **Attempt cap (N=3) reached** without green.
- **Exact human next-step:** <the one concrete action the human must take>.

---

_Safety rails honored: no auto-merge · bounded attempts · no secrets in source · minimal diffs ·
no destructive git. Generated by Self-Healing CI (#33)._
